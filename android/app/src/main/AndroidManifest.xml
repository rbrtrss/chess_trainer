<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Feature 003 added this permission, and doing so gave up something real.
         Until then the release build declared no INTERNET permission and was
         therefore *incapable* of reaching the network — a guarantee the
         operating system enforced, which feature 002's plan relied on. Importing
         studies from Lichess needs the permission, so that guarantee is gone and
         cannot be recovered.

         What replaces it is weaker and deliberate (003 research D14):
           - networking exists only in lib/data/lichess/, enforced by
             test/domain/layering_test.dart in both directions;
           - test/ui/no_network_during_training_test.dart drives the whole
             training flow against an API that fails the test if touched.
         A tested claim instead of an enforced one. Do not weaken either test to
         make something else pass. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="Chess Trainer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- The OAuth redirect lands here (003 research D3). The scheme must
             match `lichessCallbackScheme` in lib/data/lichess/lichess_auth.dart
             exactly: if the two disagree, login opens Lichess and then hangs on
             the way back with no error to explain it. -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="org.chesstrainer"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
